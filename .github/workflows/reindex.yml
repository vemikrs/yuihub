# name: Reindex (Signal stage - optional)
# on:
#   push:
#     paths:
#       - "logdocs/**"
#       - "notes/**"
# jobs:
#   build:
#     runs-on: ubuntu-latest
#     steps:
#       - uses: actions/checkout@v4
#       - uses: actions/setup-node@v4
#         with:
#           node-version: 20
#       - name: Generate Lunr index
#         run: |
#           npm ci || true
#           node scripts/build-index.js
#       - name: Commit index
#         run: |
#           git config user.name "yuihub-bot"
#           git config user.email "bot@vemi.jp"
#           git add index/lunr.idx.json
#           git commit -m "chore(index): rebuild" || echo "no changes"
#           git push
